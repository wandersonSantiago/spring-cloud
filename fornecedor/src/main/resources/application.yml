spring:
  application:
    name: 'fornecedor' 
  data:
    mongodb:
      uri: mongodb://localhost:27017/fornecedor
server:
  port: 8081
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
